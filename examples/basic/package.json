{
    "name": "examples-basic",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "node src/index.js",
        "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
    },
    "dependencies": {
        "@headless-game-engine/core": "*"
    }
}