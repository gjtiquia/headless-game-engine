{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:force": "turbo run build --force",
    "test": "turbo run test",
    "test:force": "turbo run test --force",
    "clean": "turbo clean && rm -rf node_modules && turbo daemon clean",
    "clean:daemon": "turbo daemon clean"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "tsup": "^7.0.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "turbo": "^1.10.3"
  }
}