{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:force": "turbo run build --force",
    "test": "turbo run test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "tsup": "^7.0.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "turbo": "^1.10.3"
  }
}